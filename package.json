{
  "name": "thompson-ngo",
  "version": "0.0.0",
  "scripts": {
    "dev": "coffee -c main.coffee && vite",
    "build": "coffee -c main.coffee && vite build",
    "serve": "coffee -c main.coffee && vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "coffeescript": "^2.7.0",
    "vite": "^2.3.0"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "sass": "^1.77.8",
    "three": "^0.128.0"
  }
}
