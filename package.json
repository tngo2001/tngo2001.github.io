{
  "name": "thompson-ngo",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run compile && vite",
    "build": "npm run compile && vite build",
    "serve": "npm run compile && vite preview",
    "deploy": "gh-pages -d dist",
    "compile": "pug index.pug && coffee -c main.coffee"
  },
  "devDependencies": {
    "coffeescript": "^2.7.0",
    "pug-cli": "^1.0.0-alpha6",
    "vite": "^2.3.0"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "sass": "^1.77.8",
    "three": "^0.128.0"
  }
}
